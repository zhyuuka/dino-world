# === 引擎与工具二进制（不进仓库）===
tools/
*.zip
*.tar.gz
godot
godot4

# === Android SDK / 构建产物 ===
android-sdk/
# APK 是最终交付物，需要入库让用户从 GitHub 直接下载
# *.apk  ← 不排除 APK
*.aab
*.apk.idsig
*.keystore
!debug.keystore
local.properties

# === Godot 引擎缓存（不进仓库）===
.godot/
*.imported

# === Godot 导出预设本地覆盖 ===
export_presets.cfg.local

# === 操作系统垃圾文件 ===
.DS_Store
Thumbs.db
desktop.ini

# === 编辑器/IDE 临时文件 ===
.vscode/
.idea/
*.swp
*.swo
*~

# === TRAE IDE 内部目录（规则文件可保留，其他排除）===
.trae/cache/
.trae/sessions/
.trae/state/

# === 日志和临时文件 ===
*.log
logs/
tmp/

# === 凭证文件（绝不进仓库）===
.env
.env.local
*credentials*
*token*
secrets.json
